server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # This is the magic line that fixes the 404s
    location / {
        try_files $uri $uri/ /index.html;
    }
}